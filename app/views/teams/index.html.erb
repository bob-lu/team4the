<h1>Listing teammates</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

<% @team.users.each do |user| %>
  <tr>
    <td><%= user.username %></td>
    <td><%= link_to "Profile", user %></td>
  <tr>
<% end %>
</table>